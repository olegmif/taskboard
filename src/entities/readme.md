Сущности (модели данных), которые могут использоваться в разных частях приложения.

Имеют вид

/User
    /index.ts   // Точка входа
    /model      // Модель
    /ui         // Элементы пользовательского интерфейса сущности
    /lib        // Вспомогательные утилиты