Комплексные компоненты, объединяют несколько фич или страниц в единое целое.

Видежты имеют стркутуру

/Header
    /index.ts   // Точка входа
    /ui         // Элементы UI виджета
    /lib        // Утилиты и вспомогательные функции 